{
  "name": "cronos-shield-contracts",
  "version": "1.0.0",
  "description": "Cronos Shield Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:risk-oracle": "hardhat run scripts/deploy-risk-oracle.ts --network cronosTestnet",
    "deploy:shielded-vault": "hardhat run scripts/deploy-shielded-vault.ts --network cronosTestnet",
    "authorize-oracle": "hardhat run scripts/authorize-oracle.ts --network cronosTestnet"
  },
  "keywords": [
    "cronos",
    "solidity",
    "smart-contracts",
    "risk-oracle",
    "shielded-vaults"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@typechain/ethers-v6": "^0.5.1",
    "@typechain/hardhat": "^9.1.0",
    "@types/node": "^20.14.10",
    "hardhat": "^2.22.0",
    "typescript": "^5.6.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
